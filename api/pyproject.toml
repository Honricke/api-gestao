[project]
name = "api"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "psycopg2-binary>=2.9.13",
    "sqlmodel>=0.0.42",
]

[tool.fastapi]
entrypoint = "src.main:app"
